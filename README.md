# ISP

Public backup for local ISP/proxy-related files.

Sensitive Clash configuration files are intentionally excluded from Git:

- `MiyaipShare.yaml`
- `MiyaipShare-portable.yaml`

Use `MiyaipShare.example.yaml` as the public template and keep real proxy
credentials only on trusted local devices.
